# Test Environment Deprecation Notice

- - -

## Overview

To provide more convenient and standardized services, ZEGOCLOUD has unified the environment concept. After **2021-11-16**, there is no distinction between formal environment and test environment. Users who created projects on [ZEGOCLOUD Console](https://console.zegocloud.com) on or before **2021-11-16** can refer to this document for SDK upgrades and related code adjustments.

For guidance on each product, please refer to the table below:

<table>
  <tbody><tr>
    <th>Category</th>
    <th>Product</th>
    <th>Platform/Framework</th>
  </tr>
  <tr>
    <td rowspan="3">Cloud Communication Products</td>
    <td><a href="#2_1">Video Call</a></td>
    <td><ul><li><a href="#express_video_ios">iOS</a></li><li><a href="#express_video_android">Android</a></li><li><a href="#express_video_macos">macOS</a></li><li><a href="#express_video_windows">Windows</a></li><li><a href="#express_video_web">Web</a></li><li><a href="#express_video_mini">Mini Program</a></li><li><a href="#express_video_flutter">Flutter</a></li><li><a href="#express_video_electron">Electron</a></li><li><a href="#express_video_u3d">Unity3D</a></li><li><a href="#express_video_uniapp">uni-app</a></li><li><a href="#express_video_rn">React Native</a></li></ul></td>
  </tr>
  <tr>
    <td><a href="#2_2">Voice Call</a></td>
    <td><ul><li><a href="#express_audio_ios">iOS</a></li><li><a href="#express_audio_android">Android</a></li><li><a href="#express_audio_macos">macOS</a></li><li><a href="#express_audio_windows">Windows</a></li><li><a href="#express_audio_web">Web</a></li><li><a href="#express_audio_flutter">Flutter</a></li><li><a href="#express_audio_electron">Electron</a></li><li><a href="#express_audio_u3d">Unity3D</a></li><li><a href="#express_audio_uniapp">uni-app</a></li><li><a href="#express_audio_rn">React Native</a></li></ul></td>
  </tr>
  <tr>
    <td><a href="#2_3">Live Streaming</a></td>
    <td><ul><li><a href="#l3_ios">iOS</a></li><li><a href="#l3_android">Android</a></li><li><a href="#l3_macos">macOS</a></li><li><a href="#l3_windows">Windows</a></li><li><a href="#l3_web">Web</a></li></ul></td>
  </tr>
  <tr>
    <td rowspan="4">Supporting Services/Plugins</td>
    <td><a href="#2_4">Super Board</a></td>
    <td><ul><li><a href="#super_board_ios">iOS</a></li><li><a href="#super_board_android">Android</a></li><li><a href="#super_board_web">Web</a></li></ul></td>
  </tr>
  <tr>
    <td><a href="#2_5">Interactive Whiteboard</a></td>
    <td><ul><li><a href="#whiteboard_ios">iOS</a></li><li><a href="#whiteboard_android">Android</a></li><li><a href="#whiteboard_macos">macOS</a></li><li><a href="#whiteboard_windows">Windows</a></li><li><a href="#whiteboard_web">Web</a></li><li><a href="#whiteboard_electron">Electron</a></li></ul></td>
  </tr>
  <tr>
    <td><a href="#2_5">File Sharing</a></td>
    <td><ul><li><a href="#file_share_ios">iOS</a></li><li><a href="#file_share_android">Android</a></li><li><a href="#file_share_web">Web</a></li><li><a href="#file_share_electron">Electron</a></li></ul></td>
  </tr>
  <tr>
    <td><a href="#2_6">Local Server Recording</a></td>
    <td>-</td>
  </tr>
  <tr>
    <td>Server API</td>
    <td><a href="#2_7">Server API v2</a></td>
    <td>-</td>
  </tr>
</tbody></table>



<Note title="Note">
- Products and platforms/frameworks not listed in this table are not affected by the test environment deprecation.
- You can refer to the "Sample Source Code" of the above products for corresponding code adjustments.
</Note>


## Upgrade Instructions

### Video Call

The specific implementation process for each platform/framework is as follows:

<h4 id="express_video_ios"> iOS </h4>

1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-ios-oc/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~ObjectiveC_ios~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoEngineProfile *profile = [ZegoEngineProfile new];
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenarioGeneral;

[ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
```


<h4 id="express_video_android"> Android </h4>

1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-android-java/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~Java_android~class~im-zego-zegoexpress-zego-express-engine#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```java
ZegoEngineProfile profile = new ZegoEngineProfile();
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario.GENERAL;
profile.application = getApplication();

ZegoExpressEngine.createEngine(profile, null);
```

<h4 id="express_video_macos"> macOS </h4>

- If you are using Objective-C:

    1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-macos-oc/quick-start/integrating-sdk).

    2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~ObjectiveC_macos~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

    ```objc
    ZegoEngineProfile *profile = [ZegoEngineProfile new];
    profile.appID = appID;
    profile.appSign = appSign;
    profile.scenario = ZegoScenarioGeneral;

    [ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
    ```


- If you are using C++:

    1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-macos-cpp/quick-start/integrating-sdk).

    2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~CPP_macos~class~zego-express-zego-express-sdk#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

    ```cpp
    ZegoEngineProfile profile;
    profile.appID = appID;
    profile.appSign = appSign;
    profile.scenario = ZegoScenario::ZEGO_SCENARIO_GENERAL;

    ZegoExpressSDK::createEngine(profile, nullptr);
    ```


<h4 id="express_video_windows"> Windows </h4>

- If you are using C++:

    1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-windows-cpp/quick-start/integrating-sdk).

    2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~CPP_windows~class~zego-express-zego-express-sdk#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

    ```cpp
    ZegoEngineProfile profile;
    profile.appID = appID;
    profile.appSign = appSign;
    profile.scenario = ZegoScenario::ZEGO_SCENARIO_GENERAL;

    ZegoExpressSDK::createEngine(profile, nullptr);
    ```

- If you are using C#:

    1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-windows-cs/quick-start/integrating-sdk).
    2. When initializing the SDK, please use the latest createEngine interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

    ```C#
    ZegoEngineProfile engine_profile = new ZegoEngineProfile();
    engine_profile.appID = appID;
    engine_profile.appSign = appSign;
    engine_profile.scenario = ZegoScenario.General;
    var context = SynchronizationContext.Current;

    var engine = ZegoExpressEngine.CreateEngine(engine_profile, context);
    ```


<h4 id="express_video_web"> Web </h4>


- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) on or before **2021-11-16**, the functionality for this platform is enabled by default in the test environment and can be used directly. In the formal environment, the functionality for this platform is not enabled by default and needs to be enabled through the original process if needed.
- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) after **2021-11-16**, there is no distinction between formal/test environments, but the functionality for this platform is not enabled by default. Please contact ZEGOCLOUD Technical Support to enable it before use.




<h4 id="express_video_mini"> Mini Program </h4>


- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) on or before **2021-11-16**, the functionality for this platform is enabled by default in the test environment and can be used directly. In the formal environment, the functionality for this platform is not enabled by default and needs to be enabled through the original process if needed.
- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) after **2021-11-16**, there is no distinction between formal/test environments, but the functionality for this platform is not enabled by default. Please contact ZEGOCLOUD Technical Support to enable it before use.



<h4 id="express_video_flutter"> Flutter </h4>

1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-flutter/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~Dart_flutter~class~zego-express-engine#create-engine-with-profile) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```dart
ZegoEngineProfile profile = ZegoEngineProfile(
    appID,
    appSign,
    ZegoScenario.General,
    true);

ZegoExpressEngine.createEngineWithProfile(profile);
```


<h4 id="express_video_electron"> Electron </h4>

1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-electron-js/client-sdk/download-sdk).

2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#create-engine) interface. Example code is as follows:

```js
const profile = {
appID : xxx,
appSign : "xxx",
scenario : zgDefines.ZegoScenario.General
};

zgEngine.createEngine(profile)
.then(() => {
    console.log("init succeed")
}).catch((e) => {
    console.log("init failed", e)
});
```

<h4 id="express_video_u3d"> Unity3D </h4>

1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-u3d-cs/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest CreateEngine interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```csharp
ZegoEngineProfile profile = new ZegoEngineProfile();
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario.General;

ZegoExpressEngine.CreateEngine(profile);
```

<h4 id="express_video_uniapp"> uni-app </h4>


1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-uniapp/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~JavaScript_uni-app~class~zego-express-engine#create-engine-with-profile) interface. Example code is as follows:

```js
const profile = {
appID : xxx,
appSign : "xxx",
scenario : 0
};

ZegoExpressEngine.createEngineWithProfile(profile)
```

<h4 id="express_video_rn"> React Native</h4>


1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-video-rn/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~JavaScript_react-native~class~zego-express-engine#create-engine-with-profile) interface. Example code is as follows:

```javascript
// Import
import ZegoExpressEngine from 'zego-express-engine-reactnative';

// Use general scenario
const profile = {
appID : xxx,
scenario : 0
};

ZegoExpressEngine.createEngineWithProfile(profile)
```


### Voice Call

The specific implementation process for each platform/framework is as follows:

<h4 id="express_audio_ios"> iOS </h4>

1. For integrating the latest version of the SDK, please refer to [Voice Call - Download SDK](/real-time-voice-ios/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Audio_SDK_API~ObjectiveC_ios~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoEngineProfile *profile = [ZegoEngineProfile new];
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenarioGeneral;

[ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
```


<h4 id="express_audio_android"> Android </h4>

1. For integrating the latest version of the SDK, please refer to [Voice Call - Download SDK](/real-time-voice-android/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Audio_SDK_API~Java_android~class~im-zego-zegoexpress-zego-express-engine#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```java
ZegoEngineProfile profile = new ZegoEngineProfile();
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario.GENERAL;
profile.application = getApplication();

ZegoExpressEngine.createEngine(profile, null);
```

<h4 id="express_audio_macos"> macOS </h4>

1. For integrating the latest version of the SDK, please refer to [Voice Call - Download SDK](/real-time-voice-macos/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Audio_SDK_API~ObjectiveC_macos~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoEngineProfile *profile = [ZegoEngineProfile new];
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenarioGeneral;

[ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
```



<h4 id="express_audio_windows"> Windows </h4>

1. For integrating the latest version of the SDK, please refer to [Voice Call - Download SDK](/real-time-voice-windows/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Audio_SDK_API~CPP_windows~class~zego-express-zego-express-sdk#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```cpp
ZegoEngineProfile profile;
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario::ZEGO_SCENARIO_GENERAL;

ZegoExpressSDK::createEngine(profile, nullptr);
```


<h4 id="express_audio_web"> Web </h4>


- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) on or before **2021-11-16**, the functionality for this platform is enabled by default in the test environment and can be used directly. In the formal environment, the functionality for this platform is not enabled by default and needs to be enabled through the original process if needed.
- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) after **2021-11-16**, there is no distinction between formal/test environments, but the functionality for this platform is not enabled by default. Please contact ZEGOCLOUD Technical Support to enable it before use.


<h4 id="express_audio_flutter"> Flutter </h4>

1. For integrating the latest version of the SDK, please refer to [Voice Call - Download SDK](/real-time-voice-flutter/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Audio_SDK_API~Dart_flutter~class~zego-express-engine#create-engine-with-profile) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```dart
ZegoEngineProfile profile = ZegoEngineProfile(
    appID,
    appSign,
    ZegoScenario.General,
    true);

ZegoExpressEngine.createEngineWithProfile(profile);
```


<h4 id="express_audio_electron"> Electron </h4>

1. For integrating the latest version of the SDK, please refer to [Voice Call - Download SDK](/real-time-voice-electron/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#create-engine) interface. Example code is as follows:

```js
const profile = {
appID : xxx,
appSign : "xxx",
scenario : zgDefines.ZegoScenario.General
};

zgEngine.createEngine(profile)
.then(() => {
    console.log("init succeed")
}).catch((e) => {
    console.log("init failed", e)
});
```

<h4 id="express_audio_u3d"> Unity3D </h4>

1. For integrating the latest version of the SDK, please refer to [Voice Call - Download SDK](/real-time-voice-u3d/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest CreateEngine interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```c
ZegoEngineProfile profile = new ZegoEngineProfile();
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario.General;

ZegoExpressEngine.CreateEngine(profile);
```

<h4 id="express_audio_uniapp"> uni-app </h4>


1. For integrating the latest version of the SDK, please refer to [Voice Call - Download SDK](/real-time-voice-uniapp/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Audio_SDK_API~JavaScript_uni-app~class~zego-express-engine#create-engine-with-profile) interface. Example code is as follows:

```js
const profile = {
appID : xxx,
appSign : "xxx",
scenario : 0
};

ZegoExpressEngine.createEngineWithProfile(profile)
```

<h4 id="express_audio_rn"> React Native</h4>


1. For integrating the latest version of the SDK, please refer to [Video Call - Download SDK](/real-time-voice-rn/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Audio_SDK_API~JavaScript_react-native~class~zego-express-engine#create-engine-with-profile) interface. Example code is as follows:

```javascript
// Import
import ZegoExpressEngine from 'zego-express-engine-reactnative';

// Use general scenario
const profile = {
appID : xxx,
scenario : 0
};

ZegoExpressEngine.createEngineWithProfile(profile)
```

### Live Streaming

The specific implementation process for each platform/framework is as follows:

<h4 id="l3_ios"> iOS </h4>

1. For integrating the latest version of the SDK, please refer to [Live Streaming - Download SDK](/live-streaming-ios/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~ObjectiveC_ios~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoEngineProfile *profile = [ZegoEngineProfile new];
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenarioGeneral;

[ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
```


<h4 id="l3_android"> Android </h4>

1. For integrating the latest version of the SDK, please refer to [Live Streaming - Download SDK](/live-streaming-android/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~Java_android~class~im-zego-zegoexpress-zego-express-engine#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```java
ZegoEngineProfile profile = new ZegoEngineProfile();
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario.GENERAL;
profile.application = getApplication();

ZegoExpressEngine.createEngine(profile, null);
```

<h4 id="l3_macos"> macOS </h4>

1. For integrating the latest version of the SDK, please refer to [Live Streaming - Download SDK](/live-streaming-macos/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~ObjectiveC_macos~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoEngineProfile *profile = [ZegoEngineProfile new];
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenarioGeneral;

[ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
```




<h4 id="l3_windows"> Windows </h4>

1. For integrating the latest version of the SDK, please refer to [Live Streaming - Download SDK](/live-streaming-windows/quick-start/integrating-sdk).

2. When initializing the SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~CPP_windows~class~zego-express-zego-express-sdk#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```cpp
ZegoEngineProfile profile;
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario::ZEGO_SCENARIO_GENERAL;

ZegoExpressSDK::createEngine(profile, nullptr);
```


<h4 id="l3_web"> Web </h4>


- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) on or before **2021-11-16**, the functionality for this platform is enabled by default in the test environment and can be used directly. In the formal environment, the functionality for this platform is not enabled by default and needs to be enabled through the original process if needed.
- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) after **2021-11-16**, there is no distinction between formal/test environments, but the functionality for this platform is not enabled by default. Please contact ZEGOCLOUD Technical Support to enable it before use.




### Super Board


- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) on or before **2021-11-16**, the functionality for this product is enabled by default in the test environment and can be used directly. In the formal environment, the functionality for this product is not enabled by default and needs to be enabled through the original process if needed.
- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) after **2021-11-16**, there is no distinction between formal/test environments, but the functionality for this product is not enabled by default. Please contact ZEGOCLOUD Technical Support to enable it before use.




The specific implementation process for each platform/framework is as follows:

<h4 id="super_board_ios"> iOS </h4>

1. For integrating the latest version of the SDK, please refer to [Super Board - Download SDK](/super-board-ios/quick-start/create-white-board).

2. When initializing ZegoExpress-Video SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~ObjectiveC_ios~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoEngineProfile *profile = [ZegoEngineProfile new];
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenarioGeneral;

[ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
```

3. When initializing ZegoSuperBoard SDK, use the [initWithConfig](https://www.zegocloud.com/docs/article/api?doc=superboard_API~objectivec_ios~class~ZegoSuperBoardManager%2BWhiteBoard#init-with-config) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoSuperBoardInitConfig * config = [ZegoSuperBoardInitConfig new];
config.appID = appID;
config.appSign = appSign;

[ZegoSuperBoardManager sharedInstance].delegate = self;

[[ZegoSuperBoardManager sharedInstance] initWithConfig:config complete:^(ZegoSuperBoardError errorCode) {
    if (errorCode == ZegoSuperBoardSuccess) {
         /** Initialization successful */
    } else {
        /** Initialization failed */
    }
}];
```


<h4 id="super_board_android"> Android </h4>


1. For integrating the latest version of the SDK, please refer to [Super Board - Download SDK](/super-board-android/quick-start/create-white-board).

2. When initializing ZegoExpress-Video SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~Java_android~class~im-zego-zegoexpress-zego-express-engine#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```java
ZegoEngineProfile profile = new ZegoEngineProfile();
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario.GENERAL;
profile.application = getApplication();

ZegoExpressEngine.createEngine(profile, null);
```

3. When initializing ZegoSuperBoard SDK, use the [init](https://www.zegocloud.com/docs/article/api?doc=superboard_API~java_android~class~ZegoSuperBoardManager#init) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```
ZegoSuperBoardInitConfig config = new ZegoSuperBoardInitConfig();
config.appID = appID;
config.appSign = appSign;

ZegoSuperBoardManager.getInstance().init(this, config, new IZegoSuperBoardInitCallback() {
    @Override
    public void onInit(int errorCode) {
        Log.d(TAG, "init ZegoSuperBoardManager result: errorCode = [" + errorCode + "]");
        if (errorCode == ZegoSuperBoardError.ZegoSuperBoardSuccess) {
            /** Initialization successful */
        } else {
            /** Initialization failed */
        }
    }
});
```

<h4 id="super_board_web"> Web </h4>


1. For integrating the latest version of the SDK, please refer to [Super Board - Download SDK](/super-board-web/quick-start/create-white-board).

2. When initializing ZegoSuperBoard SDK, use the [init](https://www.zegocloud.com/docs/article/api?doc=superboard_API~javascript_web~class~ZegoSuperBoardManager#init) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```
zegoSuperBoard = ZegoSuperBoardManager.getInstance();
const result = await zegoSuperBoard.init(zg, {
    parentDomID: 'parentDomID',
    appID: 0,
    userID: '',
    token: ''
});
```



### Interactive Whiteboard

The specific implementation process for each platform/framework is as follows:

<h4 id="whiteboard_ios"> iOS </h4>

1. For integrating the latest version of the SDK, please refer to [Interactive Whiteboard - Download SDK](https://www.zegocloud.com/docs/article/4406).

2. When initializing ZegoExpress-Video SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~ObjectiveC_ios~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoEngineProfile *profile = [ZegoEngineProfile new];
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenarioGeneral;

[ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
```


<h4 id="whiteboard_android"> Android </h4>

1. For integrating the latest version of the SDK, please refer to [Interactive Whiteboard - Download SDK](https://www.zegocloud.com/docs/article/4405).

2. When initializing ZegoExpress-Video SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~Java_android~class~im-zego-zegoexpress-zego-express-engine#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```java
ZegoEngineProfile profile = new ZegoEngineProfile();
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario.GENERAL;
profile.application = getApplication();

ZegoExpressEngine.createEngine(profile, null);
```

<h4 id="whiteboard_macos"> macOS </h4>

1. For integrating the latest version of the SDK, please refer to [Interactive Whiteboard - Download SDK](https://www.zegocloud.com/docs/article/6213).

2. When initializing ZegoExpress-Video SDK, please use the latest [createEngineWithProfile](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~ObjectiveC_macos~class~zego-express-engine#create-engine-with-profile-event-handler) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoEngineProfile *profile = [ZegoEngineProfile new];
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenarioGeneral;

[ZegoExpressEngine createEngineWithProfile:profile eventHandler:self];
```


<h4 id="whiteboard_windows"> Windows </h4>

1. For integrating the latest version of the SDK, please refer to [Interactive Whiteboard - Download SDK](https://www.zegocloud.com/docs/article/6435).

2. When initializing ZegoExpress-Video SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=Express_Video_SDK_API~CPP_windows~class~zego-express-zego-express-sdk#create-engine) interface. There is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```cpp
ZegoEngineProfile profile;
profile.appID = appID;
profile.appSign = appSign;
profile.scenario = ZegoScenario::ZEGO_SCENARIO_GENERAL;

ZegoExpressSDK::createEngine(profile, nullptr);
```


<h4 id="whiteboard_web"> Web </h4>

- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) on or before **2021-11-16**, the functionality for this platform is enabled by default in the test environment and can be used directly. In the formal environment, the functionality for this platform is not enabled by default and needs to be enabled through the original process if needed.
- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) after **2021-11-16**, there is no distinction between formal/test environments, but the functionality for this platform is not enabled by default. Please contact ZEGOCLOUD Technical Support to enable it before use.



<h4 id="whiteboard_electron"> Electron </h4>

1. For integrating the latest version of the SDK, please refer to [Interactive Whiteboard - Download SDK](https://www.zegocloud.com/docs/article/6497).

2. When initializing ZegoExpress-Video SDK, please use the latest [createEngine](https://www.zegocloud.com/docs/article/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#create-engine) interface. Example code is as follows:

```js
zegoExpressEngine.createEngine({ appID: zegoauth.appID, appSign: zegoauth.appSign, scenario: 0 });
```


### File Sharing

- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) on or before **2021-11-16**, the functionality for this product is enabled by default in the test environment and can be used directly. In the formal environment, the functionality for this product is not enabled by default and needs to be enabled through the original process if needed.
- For projects created on [ZEGOCLOUD Console](https://console.zegocloud.com) after **2021-11-16**, there is no distinction between formal/test environments, but the functionality for this product is not enabled by default. Please contact ZEGOCLOUD Technical Support to enable it before use.



The specific implementation process for each platform/framework is as follows:


<h4 id="file_share_ios"> iOS </h4>

1. For integrating the latest version of the SDK, please refer to [File Sharing - Download SDK](https://www.zegocloud.com/docs/article/4408).

2. When initializing the SDK, use the [initWithConfig](https://www.zegocloud.com/docs/article/api?doc=docsview_API~objectivec_ios~class~ZegoDocsViewManager#init-with-config) interface. There is no need to set isTestEnv to distinguish between formal/test environments. Example code is as follows:

```objc
ZegoDocsViewConfig *config = [ZegoDocsViewConfig new];
config.appSign = appSign;
config.appID = appID;
config.dataFolder = [[NSHomeDirectory() stringByAppendingPathComponent:@"Documents/ZegoDocs"] stringByAppendingString:@"data"];
config.cacheFolder = [[NSHomeDirectory() stringByAppendingPathComponent:@"Documents/ZegoDocs"] stringByAppendingString:@"doc"];
config.logFolder = [[NSHomeDirectory() stringByAppendingPathComponent:@"Documents/ZegoDocs"] stringByAppendingString:@"log"];

[[ZegoDocsViewManager sharedInstance] initWithConfig:config completionBlock:^(ZegoDocsViewError errorCode) {
    if (errorCode == ZegoDocsViewSuccess) {
        NSLog(@"SDK initialization successful");
    } else {
        NSLog(@"SDK initialization failed %ld",errorCode);
    }
}];
```


<h4 id="file_share_android"> Android </h4>


1. For integrating the latest version of the SDK, please refer to [File Sharing - Download SDK](https://www.zegocloud.com/docs/article/4407).

2. When initializing the SDK, use the [init](https://www.zegocloud.com/docs/article/api?doc=docsview_API~java_android~class~ZegoDocsViewManager#init) interface. There is no need to set setTestEnv to distinguish between formal/test environments. Example code is as follows:

```
ZegoDocsViewConfig config = new ZegoDocsViewConfig();
config.setAppID(appID);
config.setAppSign(appSign);

ZegoDocsViewManager.getInstance().init(application,config, new IZegoDocsViewInitListener() {
    @Override
    public void onInit(int errorCode) {
        if (errorCode == 0) {
            /** Initialization successful */
        } else {
            /** Initialization failed */
        }
    }
});
```


<h4 id="file_share_web"> Web </h4>


1. For integrating the latest version of the SDK, please refer to [File Sharing - Download SDK](https://www.zegocloud.com/docs/article/4424).

2. When initializing the SDK, there is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```js
const zegoExpressDocs = new window.ZegoExpressDocs({appID, token, userID});
```


<h4 id="file_share_electron"> Electron </h4>

1. For integrating the latest version of the SDK, please refer to [Interactive Whiteboard - Download SDK](https://www.zegocloud.com/docs/article/6498).

2. When initializing the SDK, there is no need to set the isTestEnv parameter to distinguish between formal/test environments. Example code is as follows:

```js
const zegoExpressDocs = new ZegoExpressDocs({
    appID,
    appSign,
    dataFolder,
    cacheFolder,
    logFolder
});
```


### Local Server Recording

[SetUseTestEnv](https://www.zegocloud.com/docs/API/ServerRecord/namespace_z_e_g_o_1_1_l_i_v_e_r_o_o_m.html#a67f490d5db300dffbdfc0bfde4d88076) is about to be deprecated. Please delete the relevant code for this interface. You can also refer to "How to achieve project data isolation?" in the "FAQ" section below to distinguish between formal/test environments.




### Server API v2

The common request parameter isTest for all interfaces has a default value of false, indicating the formal environment.

<ul><li>For projects created on <a target="_blank" href="https://console.zegocloud.com">ZEGOCLOUD Console</a> on or before <b>2021-11-16</b>:<ul><li>Information such as AppID and AppSign applied from the console defaults to the test environment.</li><li>You can set this parameter according to the AppId environment type and business requirements.</li></ul></li><li>For projects created on <a target="_blank" href="https://console.zegocloud.com">ZEGOCLOUD Console</a> after <b>2021-11-16</b>:<ul><li>Information such as AppID and AppSign applied from the console is all for the formal environment.</li><li>You must modify this parameter to "false" to indicate using the formal environment.</li></ul></li></ul>


## FAQ

### How to achieve project data isolation?

You can create 2 projects on [ZEGOCLOUD Console](https://console.zegocloud.com), one for testing and one for formal use, to achieve project data isolation.

For your convenience, the console also provides a "Project Phase" label. You can label the current phase of the project yourself, such as "Testing" or "Launched", so that you can distinguish between multiple projects without confusion.
