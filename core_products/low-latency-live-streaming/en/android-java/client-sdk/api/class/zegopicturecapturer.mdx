---
docType: API
---

# ZegoPictureCapturer

## ZegoPictureCapturer

*Declared in `ZegoPictureCapturer.java`*

### Methods
<ParamField
  name="setPath"
  prototype="public  void setPath(String path)"
  desc="Set the path of the picture capturer source."
  parent_file="Declared in \`ZegoPictureCapturer.java\`"
  parent_name="ZegoPictureCapturer"
  parent_type="class">
**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| path | String | The path of the picture. Support local picture file path (file://xxx), Android URI path (uri://xxx), asset resource path (asset://xxx). The URL length cannot exceed 512 characters. |

**Details**

Set the path of the picture capturer source.

<Note title="">
- **Related APIs**: User can call [createPictureCapturer] function to create a picture capturer instance.
</Note>

<Warning title="">
- **Available since**: 3.22.0
</Warning>
</ParamField>
<ParamField
  name="getIndex"
  prototype="public  int getIndex()"
  desc="Get picture capturer instance index."
  parent_file="Declared in \`ZegoPictureCapturer.java\`"
  parent_name="ZegoPictureCapturer"
  parent_type="class">
Picture capturer instance index.
</ParamField>
