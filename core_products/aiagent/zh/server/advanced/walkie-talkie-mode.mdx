# 互动模式：对讲机模式

## 场景说明

在与智能体互动时，有常见的以下几种互动形式，可以通过[创建智能体实例](/aiagent-server/api-reference/agent-instance-management/create-agent-instance)时的 `AdvancedConfig.CommunicationMode` 参数配置：

| 互动形式        | 与智能体互动的表现                                       | 常见场景                                  |
| --------------- | ------------------------------------------------------ | ----------------------------------------- |
| 全双工互动（默认） | 用户随时可以说话。                                     | AI 语音通话                                |
| 对讲机模式      | 麦克风默认关闭，并按需开启，明确用户开始说话和结束说话的时间。 | 在线会议中的临时发言、AI 硬件中的隐私保护，按住说话 |
| 倾听模式（即将上线） | 智能体仅倾听，并不会主动回复用户。                         | AI 面试，需要长时间的 AI 仅倾听用户说话而不回答的情况   |


## 实现步骤

### 前提条件
- 开通 AI Agent 服务，并按[快速发起语音通话](/aiagent-server/quick-start)完成基本流程。

### 实现不同的互动形式

<Tabs>
<Tab title="全双工模式">
按[快速发起语音通话](/aiagent-server/quick-start)完成基本流程后，可以直接与 AI 进行全双工模式互动啦，可以参考[语音识别断句](/aiagent-server/advanced/speech-segmentation-control)、[语音活动检测 VAD 灵敏度](/aiagent-server/advanced/voice-interruption-sensitivity-adjustment)等章节，实现更精细的全双工互动控制。
</Tab>
<Tab title="对讲机模式">
<Steps titleSize="p">
<Step title="创建实例时设置互动模式为对讲机模式">
通过[创建智能体实例](/aiagent-server/api-reference/agent-instance-management/create-agent-instance)时的 `AdvancedConfig.CommunicationMode` 参数配置为 2，开启对讲机模式。
</Step>
<Step title="调用服务端 API 让 AI 聆听用户说话">
调用 [StartListening](/aiagent-server/api-reference/agent-instance-control/start-listening) 接口，让 AI 聆听用户说话。
</Step>
<Step title="调用服务端 API 让 AI 停止聆听用户说话">
调用 [StopListening](/aiagent-server/api-reference/agent-instance-control/stop-listening) 接口，让 AI 停止聆听用户说话。
</Step>
</Steps>

完整调用一次让 AI 聆听用户说话后结束聆听的流程后，AI 就会根据用户说话的内容进行回复啦🎉。
</Tab>
</Tabs>


