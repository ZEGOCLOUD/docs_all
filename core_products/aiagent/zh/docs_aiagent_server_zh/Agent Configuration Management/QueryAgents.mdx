# 获取智能体详情
---

##  描述

通过向本接口，您可以获取指定智能体的详情。

## 接口原型

- 请求方法：POST
- 请求地址：`https://aigc-aiagent-api.zegotech.cn?Action=QueryAgents`
- 传输协议：HTTPS
- 调用频率限制：10 次/秒

## 请求参数

以下请求参数列表仅列出了接口请求参数和部分公共参数，完整公共参数列表请参考 [调用方式 - 公共请求参数](./../Accessing%20Server%20APIs.mdx#公共请求参数)。

| 参数               | 类型   | 是否必选 | 描述             |
|------------------|------|------|-------------------------|
| AgentIds          | Array of String | 是    | 智能体 ID 列表，长度上限为 10。 |

## 请求示例

- 请求地址 URL：

    ```https
    https://aigc-aiagent-api.zegotech.cn?Action=QueryAgents
    &<公共请求参数>
    ```

- 请求消息体：

    ```json
    {
        "AgentIds": ["xiaozhi"]
    }
    ```
## 响应参数

| 参数 | 类型 | 描述 |
|------|------|------|
| Code | Number | 返回码。0 表示成功，其他值表示失败。如需了解错误码及响应处理建议，请参考 [返回码](./../Return%20codes.mdx)。 |
| Message | String | 请求结果的说明信息。 |
| RequestId | String | 请求 ID。 |
| Data | Object | 返回数据。 |
| └Agents | Array of Object | 智能体列表。本参数的结构，请参 [常用参数说明 - Agents](./../Common%20Parameter%20Description.mdx#agents)。

## 响应示例

```json
{
    "Code": 0,
    "Message": "success",
    "RequestId": "3201747060425215794",
    "Data": {
        "Agents": [
            {
                "AgentId": "xiaozhi",
                "AgentConfig": {
                    "Name": "小智",
                    "LLM": {
                        "Url": "https://ark.cn-beijing.volces.com/api/v3/chat/completions",
                        "ApiKey": "eexxxxxxxxxxxxxxx",
                        "Model": "ep-xxxxxxxxxx",
                        "SystemPrompt": "你是小智，成年女性，是**即构科技创造的陪伴助手**，上知天文下知地理，聪明睿智、热情友善。\n对话要求：1、按照人设要求与用户对话。\n2、不能超过100字。"
                    },
                    "TTS": {
                        "Vendor": "Bytedance",
                        "Params": {
                            "app": {
                                "appid": "your_appid",
                                "cluster": "volcano_tts",
                                "token": "your_token"
                            },
                            "audio": {
                                "emotion": "your_emotion",
                                "enable_emotion": true,
                                "speed_ratio": 1,
                                "voice_type": "your_voice_type"
                            }
                        }
                    },
                    "ASR": {}
                }
            }
        ]
    }
}
```