---
docType: API
date: "2026-06-24"
---

# Class

| | |
| --- | --- |
|[ZPNs](#zpns)||

## ZPNs

**详情**

ZPNs SDK 主类。

### 方法
<ParamField
  name="getInstance"
  prototype="  getInstance(): [ZPNs](#zpns)"
  desc="获得 ZPNs 实例。"
  prefixes={["static"]}
  parent_name="ZPNs"
  parent_type="class">
获取 ZPNs 实例。

<Note title="">
- **业务场景**：使用 ZPNs 时需要通过此方法获取 ZPNs 单例对象。 
</Note>

<Warning title="">
- **支持版本**：2.3.0 及以上版本。
</Warning>

ZPNs 实例对象。
</ParamField>
<ParamField
  name="register"
  prototype="  register(config: [ZPNsConfig](./interface#zpnsconfig), zim: [ZIM](#zim)): void"
  desc="该方法用于注册厂商离线推送。"
  parent_name="ZPNs"
  parent_type="class">
**参数**

| 名称 | 类型 | 描述 |
| --- | --- | --- |
| config | [ZPNsConfig](./interface#zpnsconfig) | 离线推送配置对象。 |
| zim | [ZIM](#zim) | ZIM 实例，ZIM SDK 需要在 2.3.0 或以上版本。 |

**详情**

该方法用于注册谷歌 FCM 离线推送。

<Note title="">
- **业务场景**：当开发者需要使用离线推送时可调用该方法。
</Note>

<Warning title="">
- **支持版本**：ZPNs 2.3.0 及以上版本。
</Warning>
</ParamField>
<ParamField
  name="unregister"
  prototype="  unregister(): void"
  desc="不需要离线推送时可调用此方法反注册。"
  parent_name="ZPNs"
  parent_type="class">
不需要离线推送时可调用此方法反注册。

<Note title="">
- **调用时机/通知时机**：需要反注册时调用。
</Note>

<Warning title="">
- **支持版本**：2.3.0 及以上版本。
- **使用限制**：需要在 [register] 调用后才可调用。
</Warning>
</ParamField>
