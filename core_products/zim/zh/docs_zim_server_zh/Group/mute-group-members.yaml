openapi: 3.0.0
info:
  title: open-api-desc
  version: 1.0.0
  contact:
    name: ZEGO Support
    email: support@zegocloud.com

servers:
  $ref: '../shared-components.yaml#/servers'

tags:
  - group

paths:
  /:
    get:
      tags: [group]
      summary: 设置群成员禁言
      description: |
        通过该接口可以禁言或解禁群组内的特定成员。

        接口调用成功后，群组内全体用户通过以下 ZIM SDK 的回调接口，得知哪些群成员无法在该群组发言或解除禁言状态：

        | iOS-25% | Android-25% | macOS-25% | Windows-25% |
        |-----|---------|-------|-------|
        | [groupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~objective-c_ios~protocol~ZIMEventHandler#zim-group-member-info-updated-operated-info-group-id) | [onGroupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~java_android~class~ZIMEventHandler#on-group-member-info-updated) | [groupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~objective-c_macos~protocol~ZIMEventHandler#zim-group-member-info-updated-operated-info-group-id) | [onGroupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~cpp_windows~class~ZIMEventHandler#on-group-member-info-updated) |

        | Web-25% | 小程序-25% | Flutter-25% | React Native-25% |
        |---------|-----|---------|---------|
        | [groupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_web~interface~ZIMEventHandler#group-member-info-updated) | [groupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_wxxcx~interface~ZIMEventHandler#group-member-info-updated) | [onGroupMemberInfoUpdated](https://pub.dev/documentation/zego_zim/latest/zego_zim/ZIMEventHandler/onGroupMemberInfoUpdated.html) | [groupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_react-native~interface~ZIMEventHandler#group-member-info-updated) |

        | Unity3D-25% | uni-app \| uni-app x-25% | HarmonyOS-25% |
        |---------|---------|-----------|
        | [OnGroupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~cs_unity3d~class~ZIMEventHandler#on-group-member-info-updated) | [groupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_uni-app~interface~ZIMEventHandler#group-member-info-updated) | [groupMemberInfoUpdated](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_harmony~interface~ZIMEventHandler#group-member-info-updated) |

        <Note title="说明">参数 `FromUserId` 和 `UserId` 对应的用户已在客户端调用 `login` 方法登录 ZIM 服务，或已调用 [服务端 API](/zim-server/user/batch-register-users) 完成注册。</Note>

        <Note title="说明">参数 FromUserId 和 UserIds 仅支持数字，英文字符和 `'!','#','$','%','&','(',')','+','\'',':',';','<','=','.','>','?',
        '@','[',']','^','_','{','}','|','~'`。</Note>

        <Note title="说明">调用频率限制：20 次/秒。</Note>
      operationId: mute-group-members
      parameters:
        - name: Action
          in: query
          description: |
            > 接口原型参数
            >
            > https://zim-api.zego.im/?Action=MuteGroupMembers
          required: true
          schema:
            type: string
            enum: [MuteGroupMembers]
          style: form
          explode: true
        - $ref: '../shared-components.yaml#/components/parameters/AppId'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureNonce'
        - $ref: '../shared-components.yaml#/components/parameters/Timestamp'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureVersion'
        - $ref: '../shared-components.yaml#/components/parameters/Signature'
        - name: FromUserId
          in: query
          required: true
          description: |
            操作用户 ID（已在客户端调用 login 方法登录 ZIM 服务，或已调用 [服务端 API](/zim-server/user/batch-register-users) 完成注册）。

            > **说明**
            >
            > 由于禁言操作会触发群 tips 消息，因此当 FromUserId 不是群组成员时，开发者需要隐藏该消息或实现其他处理，避免群内用户产生困惑。
          schema:
            type: string
            maxLength: 32
            example: "operator_user"
        - name: GroupId
          in: query
          required: true
          description: 群组 ID。
          schema:
            type: string
            maxLength: 32
            example: "group123"
        - name: UserIds
          in: query
          required: true
          description: 禁言或解禁群成员用户 ID 列表，单次请求最多传入 20 个用户 ID。
          schema:
            type: array
            maxItems: 20
            items:
              type: string
              maxLength: 32
              example: "user1"
          style: form
          explode: true
        - name: Duration
          in: query
          required: false
          description: |
            禁言时长，不传默认为 0。
            - -1：永久。
            - 0：取消禁言。
            - 大于 0：禁言时长，单位为秒。最大支持 30 天（2592000 秒）。

            若在某位群用户的禁言有效期内，再次调用本接口更新禁言时长，则以该请求时刻起重新计算禁言时间。
          schema:
            type: number
            minimum: -1
            maximum: 2592000
            example: 3600
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MuteGroupMembersResponse'

components:
  schemas:
    MuteGroupMembersResponse:
      type: object
      properties:
        Code:
          type: number
          description: |
            返回码。

            以下仅列出了接口业务逻辑相关的 Code 和 SubCode ，完整返回码请参考 [全局返回码](/zim-server/return-codes)。
            <table>
            <thead><tr><th>Code / SubCode</th><th>说明</th><th>处理建议</th></tr></thead>
            <tbody>
            <tr><td>660000001</td><td>业务类通用错误。</td><td>请重试，或联系 ZEGO 技术支持。</td></tr>
            <tr><td>660000002</td><td>参数错误。</td><td>请检查请求参数。</td></tr>
            <tr><td>660300005</td><td>调用接口的频率超出了 AppID 级别限制。</td><td>请稍后再试。</td></tr>
            <tr><td>660600001</td><td>群组不存在。</td><td>请确认传入的 GroupID 是否正确。</td></tr>
            <tr><td>660600009</td><td>获取群相关信息失败。</td><td>请先确认 GroupID 是否正确。如果正确，请联系 ZEGO 技术支持。</td></tr>
            <tr><td>660600024</td><td>FromUserId 不在群内。</td><td>请检查  FromUserId 或 或添加 FromUserId 为群成员。</td></tr>
            <tr><td>660600032</td><td>FromUserId 没权限。</td><td>FromUserId 需为群主或管理员。</td></tr>
            <tr><td>660600033</td><td>全部指定用户操作均失败。</td><td>请检查 UserIds。</td></tr>
            </tbody>
            </table>
          example: 0
        Message:
          type: string
          description: 操作结果描述。
          example: "success"
        RequestId:
          type: string
          description: 请求 ID。
          example: "343649807833778782"
        ErrorUsers:
          type: array
          description: |
            失败用户列表。
            <ul>
            <li>Code 为 0：</li>
            <li>ErrorUsers 为空，全部指定用户禁言/解禁成功。</li>
            <li>ErrorUsers 不为空，表示部分指定用户禁言/解禁失败，请参考 SubCode 处理。</li>
            <li>Code 不为 0：</li>
            <li>ErrorUsers 为空，表示参数错误、接口频率限制、系统错误。</li>
            <li>ErrorUsers 不为空，表示全部指定用户禁言/解禁失败。</li>
            </ul>
          items:
            type: object
            properties:
              UserId:
                type: string
                description: 失败用户 ID。
              SubCode:
                type: number
                description: |
                  失败的具体返回码。完整返回码请参考 Code 说明或者 [全局返回码](/zim-server/return-codes)。
