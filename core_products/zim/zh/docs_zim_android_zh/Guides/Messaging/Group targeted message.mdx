---
articleID: 17429
---

import { getPlatformData } from "/snippets/utils-content-parser.js"
import { ZIMTextMessageMap, setEventHandlerMap, groupMessageReceivedMap, ZIMMessageMap } from "/snippets/zim/const-link.mdx"

# 群组定向消息

- - -

## 功能简介

ZIM SDK 支持用户在群聊中发送一条仅部分群成员可见或仅部分群成员不可见的消息。
<Note title="注意">

群定向消息目前只能由服务端接口发送。
群定向消息不会被纳入未读数计算。且不可回复，表态，回执，编辑。
</Note>

## 接收定向消息

**成功登录 ZIM SDK 后**，当本用户在某个群聊内接收到一条自己可见的群定向消息时，会触发 {getPlatformData(props,groupMessageReceivedMap)} 回调。且收到的 {getPlatformData(props,ZIMMessageMap)} 中，<code>isGroupTargetedMessage</code> 的值为 <code>true</code>。

接收消息参考文档：[收发消息文档](./Send%20and%20receive%20messages.mdx#接收消息)

群定向消息可见性如下：

<table>
<tbody><tr>
<th>用户是否为发送者</th>
<th>定向消息为指定可见或不可见</th>
<th>用户列表是否包含本用户</th>
<th>是否可见</th>
</tr>
<tr>
<td>发送者</td>
<td> - </td>
<td> 发送者不可被包含在指定用户列表中 </td>
<td>可见</td>
</tr>
<tr>
<td>非发送者</td>
<td>可见</td>
<td>包含</td>
<td>可见</td>
</tr>
<tr>
<td>非发送者</td>
<td>可见</td>
<td>不包含</td>
<td>不可见</td>
</tr>
<tr>
<td>非发送者</td>
<td>不可见</td>
<td>包含</td>
<td>不可见</td>
</tr>
<tr>
<td>非发送者</td>
<td>不可见</td>
<td>不包含</td>
<td>可见</td>
</tr>
</tbody>
</table>
