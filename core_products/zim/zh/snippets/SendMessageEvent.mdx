import {getPlatformData2 } from "/snippets/utils-content-parser.js"
import {messageReceivedMap, sendMessageMap} from "/snippets/zim/const-link.mdx"

<Note title="说明">
- 发送消息时，统一使用 {getPlatformData2(props, sendMessageMap)} 接口，并根据会话类型传入对应的 conversationType 取值。
- 接收消息时：
  + 通过 {getPlatformData2(props, messageReceivedMap)} 回调接收所有类型的消息（单聊、群组、房间），并通过 conversationType 区分消息来源。
</Note>
