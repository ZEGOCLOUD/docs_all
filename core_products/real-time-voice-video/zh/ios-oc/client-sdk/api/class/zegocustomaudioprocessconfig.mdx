---
docType: API
---

# ZegoCustomAudioProcessConfig

## ZegoCustomAudioProcessConfig

自定义音频处理配置对象

**详情**

包括自定义音频采集类型，采样率，通道数，采样数等参数

*Declared in `ZegoExpressDefines.h`*

### 属性
<ParamField
  name="sampleRate"
  prototype="nonatomic, assign [ZegoAudioSampleRate](../../api-reference/enum#zegoaudiosamplerate) sampleRate"
  parent_name="ZegoCustomAudioProcessConfig"
  parent_type="class">
采样率，App 中音频前处理模块期望的输入数据采样率。如果为 0，则默认为 SDK 内部采样率。
</ParamField>
<ParamField
  name="channel"
  prototype="nonatomic, assign [ZegoAudioChannel](../../api-reference/enum#zegoaudiochannel) channel"
  parent_name="ZegoCustomAudioProcessConfig"
  parent_type="class">
声道数，App 中音频前处理模块期望的输入数据声道数。如果为 0，则默认为 SDK 内部声道数
</ParamField>
<ParamField
  name="samples"
  prototype="nonatomic, assign int samples"
  parent_name="ZegoCustomAudioProcessConfig"
  parent_type="class">
编码一帧所需要的采样数；如果 samples = 0，则使用 SDK 内部采样数，SDK 将音频数据传递给外部前处理模块；如果 samples != 0（则samples 的有效取值在 [160, 2048] 间），SDK 会传递设定采样数长度的音频数据给外部预处理模块。
</ParamField>
