---
docType: API
---

# ZegoPositionOrientation

## ZegoPositionOrientation

朝向。

*Declared in `entity/ZegoPositionOrientation.java`*

### 属性
<ParamField
  name="axisForward"
  prototype="public float[] axisForward"
  parent_name="ZegoPositionOrientation"
  parent_type="class">
自身坐标系前轴的单位向量，参数是长度为 3 的 float 数组，三个值依次表示前、右、上的坐标值。
</ParamField>
<ParamField
  name="axisRight"
  prototype="public float[] axisRight"
  parent_name="ZegoPositionOrientation"
  parent_type="class">
自身坐标系右轴的单位向量，参数是长度为 3 的 float 数组，三个值依次表示前、右、上的坐标值。
</ParamField>
<ParamField
  name="axisUp"
  prototype="public float[] axisUp"
  parent_name="ZegoPositionOrientation"
  parent_type="class">
自身坐标系上轴的单位向量，参数是长度为 3 的 float 数组，三个值依次表示前、右、上的坐标值。
</ParamField>
