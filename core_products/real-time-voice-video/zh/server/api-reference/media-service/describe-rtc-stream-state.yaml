openapi: 3.0.0
info:
  title: open-api-desc
  version: 1.0.0
  contact: { name: Real-time Voice/Video API Support, email: support@zegocloud.com }

tags:
  - name: media-service

servers:
  $ref: '../shared-components.yaml#/servers'

paths:
  /:
    get:
      tags: [media-service]
      summary: DescribeRTCStreamState
      description: 查询指定流的实时状态（是否活跃或存在）。
      operationId: describe-rtc-stream-state
      parameters:
        - name: Action
          in: query
          description: |
            > 接口原型参数
            >
            > https://rtc-api.zego.im?Action=DescribeRTCStreamState
          required: true
          schema: { type: string, enum: [DescribeRTCStreamState] }
          style: form
          explode: true
        - $ref: '../shared-components.yaml#/components/parameters/AppId'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureNonce'
        - $ref: '../shared-components.yaml#/components/parameters/Timestamp'
        - $ref: '../shared-components.yaml#/components/parameters/Signature'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureVersion'
        - $ref: '../shared-components.yaml#/components/parameters/IsTest'
        - name: StreamId
          in: query
          required: true
          description: 流 ID。
          schema: { type: string }
        - name: Sequence
          in: query
          required: true
          description: 请求序列号，由用户自定义。并发情况下须在 10 秒内严格递增。
          schema: { type: string }
      responses:
        "200":
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DescribeRTCStreamStateResponse'

components:
  schemas:
    DescribeRTCStreamStateResponse:
      type: object
      properties:
        Code:
          type: integer
          format: int32
          description: 返回码。0 表示查询成功且流存在并活跃；其他为失败。
        Message: { type: string }
        RequestId: { type: string }
        Data:
          type: object
          properties:
            create_time: { type: string }
            close_time: { type: string }

