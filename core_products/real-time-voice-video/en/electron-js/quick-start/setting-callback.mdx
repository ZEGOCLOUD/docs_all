---
articleID: 21395
---
# Setting Callbacks

- - -

<Warning title="Note">


This document is only used to explain the callback setting method and parameter usage for the Electron platform. For detailed **function introduction** and **parameter description**, please refer to the specific API documentation.

</Warning>



## Module Description
Express Electron SDK manages callbacks across multiple modules. Modules and their instance acquisition methods are as follows:

1. [ZegoExpressEngine](/real-time-video-electron-js/quick-start/setting-callback)

    Create SDK instance zegoEngine
    ```js
    // Create SDK instance
    const zgEngine = window.require('zego-express-engine-electron/ZegoExpressEngine');
    ```

2. [ZegoExpressMediaPlayer](/real-time-video-electron-js/quick-start/setting-callback)

    [createMediaPlayer](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#create-media-player) --- Create media player instance zgMediaPlayer
    ```js
    // Create media player instance
    const zgMediaPlayer = zgEngine.createMediaPlayer();
    ```

3. [ZegoExpressAudioEffectPlayer](/real-time-video-electron-js/quick-start/setting-callback)

    [createAudioEffectPlayer](https://www.zegocloud.com/docs/api?doc=Express_Video_SDK_API~javascript_electron~class~ZegoExpressEngine#create-audio-effect-player) --- Create audio effect player instance zgAudioEffectPlayer

    ```js
    // Create audio effect player instance
    const zgAudioEffectPlayer = zgEngine.createAudioEffectPlayer();
    ```

4. [ZegoExpressScreenCaptureSource](/real-time-video-electron-js/quick-start/setting-callback)

    [createScreenCaptureSource](https://www.zegocloud.com/docs/api?doc=Express_Video_SDK_API~javascript_electron~class~ZegoExpressEngine#create-screen-capture-source) --- Create screen capture source instance zgScreenCaptureSource

    ```js
    // Create screen capture source instance
    // sourceId  {number} Specified screen ID or window ID
    // sourceType  {ZegoScreenCaptureSourceType} Specified screen source type.
    const zgScreenCaptureSource = zgEngine.createScreenCaptureSource(sourceId, sourceType);
    ```

## Register Callbacks
### ZegoExpressEngine

<a id="onDebugError"></a>

#### [onDebugError](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-debug-error)

Usage example:

```js
zgEngine.on("onDebugError",({errorCode,funcName,info})=>{
  console.log(`onDebugError: ${errorCode},${funcName},${info}`)
})
```

<a id="onEngineStateUpdate"></a>

#### [onEngineStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-engine-state-update)

Usage example:

```js
zgEngine.on("onEngineStateUpdate",({state})=>{
  console.log(`onEngineStateUpdate: ${state}`)
})
```

<a id="onRecvExperimentalAPI"></a>

#### [onRecvExperimentalAPI](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-recv-experimental-api)

Usage example:

```js
zgEngine.on("onRecvExperimentalAPI",({content})=>{
  console.log(`onRecvExperimentalAPI: ${content}`)
})
```

<a id="onRoomStateUpdate"></a>

#### [onRoomStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-room-state-update)

Usage example:

```js
zgEngine.on("onRoomStateUpdate",({roomID,state,errorCode,extendedData})=>{
  console.log(`onRoomStateUpdate: ${roomID},${state},${errorCode},${extendedData}`)
})
```

<a id="onRoomStateChanged"></a>

#### [onRoomStateChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-room-state-changed)

Usage example:

```js
zgEngine.on("onRoomStateChanged",({roomID,reason,errorCode,extendedData})=>{
  console.log(`onRoomStateChanged: ${roomID},${reason},${errorCode},${extendedData}`)
})
```

<a id="onRoomUserUpdate"></a>

#### [onRoomUserUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-room-user-update)

Usage example:

```js
zgEngine.on("onRoomUserUpdate",({roomID,updateType,userList})=>{
  console.log(`onRoomUserUpdate: ${roomID},${updateType},${userList}`)
})
```

<a id="onRoomOnlineUserCountUpdate"></a>

#### [onRoomOnlineUserCountUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-room-online-user-count-update)

Usage example:

```js
zgEngine.on("onRoomOnlineUserCountUpdate",({roomID,count})=>{
  console.log(`onRoomOnlineUserCountUpdate: ${roomID},${count}`)
})
```

<a id="onRoomStreamUpdate"></a>

#### [onRoomStreamUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-room-stream-update)

Usage example:

```js
zgEngine.on("onRoomStreamUpdate",({roomID,updateType,streamList,extendedData})=>{
  console.log(`onRoomStreamUpdate: ${roomID},${updateType},${streamList},${extendedData}`)
})
```

<a id="onRoomStreamExtraInfoUpdate"></a>

#### [onRoomStreamExtraInfoUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-room-stream-extra-info-update)

Usage example:

```js
zgEngine.on("onRoomStreamExtraInfoUpdate",({roomID,streamList})=>{
  console.log(`onRoomStreamExtraInfoUpdate: ${roomID},${streamList}`)
})
```

<a id="onRoomExtraInfoUpdate"></a>

#### [onRoomExtraInfoUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-room-extra-info-update)

Usage example:

```js
zgEngine.on("onRoomExtraInfoUpdate",({roomID,roomExtraInfoList})=>{
  console.log(`onRoomExtraInfoUpdate: ${roomID},${roomExtraInfoList}`)
})
```

<a id="onRoomTokenWillExpire"></a>

#### [onRoomTokenWillExpire](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-room-token-will-expire)

Usage example:

```js
zgEngine.on("onRoomTokenWillExpire",({roomID,remainTimeInSecond})=>{
  console.log(`onRoomTokenWillExpire: ${roomID},${remainTimeInSecond}`)
})
```

<a id="onPublisherStateUpdate"></a>

#### [onPublisherStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-publisher-state-update)

Usage example:

```js
zgEngine.on("onPublisherStateUpdate",({streamID,state,errorCode,extendedData})=>{
  console.log(`onPublisherStateUpdate: ${streamID},${state},${errorCode},${extendedData}`)
})
```

<a id="onPublisherQualityUpdate"></a>

#### [onPublisherQualityUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-publisher-quality-update)

Usage example:

```js
zgEngine.on("onPublisherQualityUpdate",({streamID,quality})=>{
  console.log(`onPublisherQualityUpdate: ${streamID},${quality}`)
})
```

<a id="onPublisherCapturedAudioFirstFrame"></a>

#### [onPublisherCapturedAudioFirstFrame](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-publisher-captured-audio-first-frame)

Usage example:

```js
zgEngine.on("onPublisherCapturedAudioFirstFrame",()=>{
  console.log(`onPublisherCapturedAudioFirstFrame: `)
})
```

<a id="onPublisherCapturedVideoFirstFrame"></a>

#### [onPublisherCapturedVideoFirstFrame](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-publisher-captured-video-first-frame)

Usage example:

```js
zgEngine.on("onPublisherCapturedVideoFirstFrame",({channel})=>{
  console.log(`onPublisherCapturedVideoFirstFrame: ${channel}`)
})
```

<a id="onPublisherSendAudioFirstFrame"></a>

#### [onPublisherSendAudioFirstFrame](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-publisher-send-audio-first-frame)

Usage example:

```js
zgEngine.on("onPublisherSendAudioFirstFrame",({channel})=>{
  console.log(`onPublisherSendAudioFirstFrame: ${channel}`)
})
```

<a id="onPublisherSendVideoFirstFrame"></a>

#### [onPublisherSendVideoFirstFrame](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-publisher-send-video-first-frame)

Usage example:

```js
zgEngine.on("onPublisherSendVideoFirstFrame",({channel})=>{
  console.log(`onPublisherSendVideoFirstFrame: ${channel}`)
})
```

<a id="onPublisherVideoSizeChanged"></a>

#### [onPublisherVideoSizeChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-publisher-video-size-changed)

Usage example:

```js
zgEngine.on("onPublisherVideoSizeChanged",({width,height,channel})=>{
  console.log(`onPublisherVideoSizeChanged: ${width},${height},${channel}`)
})
```

<a id="onPublisherRelayCDNStateUpdate"></a>

#### [onPublisherRelayCDNStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-publisher-relay-cdn-state-update)

Usage example:

```js
zgEngine.on("onPublisherRelayCDNStateUpdate",({streamID,infoList})=>{
  console.log(`onPublisherRelayCDNStateUpdate: ${streamID},${infoList}`)
})
```

<a id="onVideoObjectSegmentationStateChanged"></a>

#### [onVideoObjectSegmentationStateChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-video-object-segmentation-state-changed)

Usage example:

```js
zgEngine.on("onVideoObjectSegmentationStateChanged",({state,channel,errorCode})=>{
  console.log(`onVideoObjectSegmentationStateChanged: ${state},${channel},${errorCode}`)
})
```

<a id="onPlayerStateUpdate"></a>

#### [onPlayerStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-state-update)

Usage example:

```js
zgEngine.on("onPlayerStateUpdate",({streamID,state,errorCode,extendedData})=>{
  console.log(`onPlayerStateUpdate: ${streamID},${state},${errorCode},${extendedData}`)
})
```

<a id="onPlayerQualityUpdate"></a>

#### [onPlayerQualityUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-quality-update)

Usage example:

```js
zgEngine.on("onPlayerQualityUpdate",({streamID,quality})=>{
  console.log(`onPlayerQualityUpdate: ${streamID},${quality}`)
})
```

<a id="onPlayerMediaEvent"></a>

#### [onPlayerMediaEvent](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-media-event)

Usage example:

```js
zgEngine.on("onPlayerMediaEvent",({streamID,event})=>{
  console.log(`onPlayerMediaEvent: ${streamID},${event}`)
})
```

<a id="onPlayerRecvAudioFirstFrame"></a>

#### [onPlayerRecvAudioFirstFrame](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-recv-audio-first-frame)

Usage example:

```js
zgEngine.on("onPlayerRecvAudioFirstFrame",({streamID})=>{
  console.log(`onPlayerRecvAudioFirstFrame: ${streamID}`)
})
```

<a id="onPlayerRecvVideoFirstFrame"></a>

#### [onPlayerRecvVideoFirstFrame](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-recv-video-first-frame)

Usage example:

```js
zgEngine.on("onPlayerRecvVideoFirstFrame",({streamID})=>{
  console.log(`onPlayerRecvVideoFirstFrame: ${streamID}`)
})
```

<a id="onPlayerRenderVideoFirstFrame"></a>

#### [onPlayerRenderVideoFirstFrame](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-render-video-first-frame)

Usage example:

```js
zgEngine.on("onPlayerRenderVideoFirstFrame",({streamID})=>{
  console.log(`onPlayerRenderVideoFirstFrame: ${streamID}`)
})
```

<a id="onPlayerVideoSizeChanged"></a>

#### [onPlayerVideoSizeChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-video-size-changed)

Usage example:

```js
zgEngine.on("onPlayerVideoSizeChanged",({streamID,width,height})=>{
  console.log(`onPlayerVideoSizeChanged: ${streamID},${width},${height}`)
})
```

<a id="onPlayerRecvSEI"></a>

#### [onPlayerRecvSEI](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-recv-sei)

Usage example:

```js
zgEngine.on("onPlayerRecvSEI",({streamID,data})=>{
  console.log(`onPlayerRecvSEI: ${streamID},${data}`)
})
```

<a id="onPlayerSyncRecvSEI"></a>

#### [onPlayerSyncRecvSEI](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-player-sync-recv-sei)

Usage example:

```js
zgEngine.on("onPlayerSyncRecvSEI",({streamID,data})=>{
  console.log(`onPlayerSyncRecvSEI: ${streamID},${data}`)
})
```

<a id="onAudioDeviceStateChanged"></a>

#### [onAudioDeviceStateChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-audio-device-state-changed)

Usage example:

```js
zgEngine.on("onAudioDeviceStateChanged",({updateType,deviceType,deviceInfo})=>{
  console.log(`onAudioDeviceStateChanged: ${updateType},${deviceType},${deviceInfo}`)
})
```

<a id="onAudioDeviceVolumeChanged"></a>

#### [onAudioDeviceVolumeChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-audio-device-volume-changed)

Usage example:

```js
zgEngine.on("onAudioDeviceVolumeChanged",({deviceType,deviceID,volume})=>{
  console.log(`onAudioDeviceVolumeChanged: ${deviceType},${deviceID},${volume}`)
})
```

<a id="onVideoDeviceStateChanged"></a>

#### [onVideoDeviceStateChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-video-device-state-changed)

Usage example:

```js
zgEngine.on("onVideoDeviceStateChanged",({updateType,deviceInfo})=>{
  console.log(`onVideoDeviceStateChanged: ${updateType},${deviceInfo}`)
})
```

<a id="onCapturedSoundLevelUpdate"></a>

#### [onCapturedSoundLevelUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-captured-sound-level-update)

Usage example:

```js
zgEngine.on("onCapturedSoundLevelUpdate",({soundLevel})=>{
  console.log(`onCapturedSoundLevelUpdate: ${soundLevel}`)
})
```

<a id="onCapturedSoundLevelInfoUpdate"></a>

#### [onCapturedSoundLevelInfoUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-captured-sound-level-info-update)

Usage example:

```js
zgEngine.on("onCapturedSoundLevelInfoUpdate",({soundLevelInfo})=>{
  console.log(`onCapturedSoundLevelInfoUpdate: ${soundLevelInfo}`)
})
```

<a id="onRemoteSoundLevelUpdate"></a>

#### [onRemoteSoundLevelUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-remote-sound-level-update)

Usage example:

```js
zgEngine.on("onRemoteSoundLevelUpdate",({soundLevels})=>{
  console.log(`onRemoteSoundLevelUpdate: ${soundLevels}`)
})
```

<a id="onLocalCaptureVideoData"></a>

#### [onLocalCaptureVideoData](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-local-capture-video-data)

Usage example:

```js
zgEngine.on("onLocalCaptureVideoData",({videoParam})=>{
  console.log(`onLocalCaptureVideoData: ${videoParam}`)
})
```

<a id="onRemotePlayVideoData"></a>

#### [onRemotePlayVideoData](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-remote-play-video-data)

Usage example:

```js
zgEngine.on("onRemotePlayVideoData",({videoParam})=>{
  console.log(`onRemotePlayVideoData: ${videoParam}`)
})
```

<a id="onRemoteSoundLevelInfoUpdate"></a>

#### [onRemoteSoundLevelInfoUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-remote-sound-level-info-update)

Usage example:

```js
zgEngine.on("onRemoteSoundLevelInfoUpdate",({soundLevelInfos})=>{
  console.log(`onRemoteSoundLevelInfoUpdate: ${soundLevelInfos}`)
})
```

<a id="onCapturedAudioSpectrumUpdate"></a>

#### [onCapturedAudioSpectrumUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-captured-audio-spectrum-update)

Usage example:

```js
zgEngine.on("onCapturedAudioSpectrumUpdate",({audioSpectrum})=>{
  console.log(`onCapturedAudioSpectrumUpdate: ${audioSpectrum}`)
})
```

<a id="onRemoteAudioSpectrumUpdate"></a>

#### [onRemoteAudioSpectrumUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-remote-audio-spectrum-update)

Usage example:

```js
zgEngine.on("onRemoteAudioSpectrumUpdate",({audioSpectrums})=>{
  console.log(`onRemoteAudioSpectrumUpdate: ${audioSpectrums}`)
})
```

<a id="onLocalDeviceExceptionOccurred"></a>

#### [onLocalDeviceExceptionOccurred](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-local-device-exception-occurred)

Usage example:

```js
zgEngine.on("onLocalDeviceExceptionOccurred",({exceptionType,deviceType,deviceID})=>{
  console.log(`onLocalDeviceExceptionOccurred: ${exceptionType},${deviceType},${deviceID}`)
})
```

<a id="onRemoteCameraStateUpdate"></a>

#### [onRemoteCameraStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-remote-camera-state-update)

Usage example:

```js
zgEngine.on("onRemoteCameraStateUpdate",({streamID,state})=>{
  console.log(`onRemoteCameraStateUpdate: ${streamID},${state}`)
})
```

<a id="onRemoteMicStateUpdate"></a>

#### [onRemoteMicStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-remote-mic-state-update)

Usage example:

```js
zgEngine.on("onRemoteMicStateUpdate",({streamID,state})=>{
  console.log(`onRemoteMicStateUpdate: ${streamID},${state}`)
})
```

<a id="onRemoteSpeakerStateUpdate"></a>

#### [onRemoteSpeakerStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-remote-speaker-state-update)

Usage example:

```js
zgEngine.on("onRemoteSpeakerStateUpdate",({streamID,state})=>{
  console.log(`onRemoteSpeakerStateUpdate: ${streamID},${state}`)
})
```

<a id="onAudioVADStateUpdate"></a>

#### [onAudioVADStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-audio-vad-state-update)

Usage example:

```js
zgEngine.on("onAudioVADStateUpdate",({type,state})=>{
  console.log(`onAudioVADStateUpdate: ${type},${state}`)
})
```

<a id="onIMRecvBroadcastMessage"></a>

#### [onIMRecvBroadcastMessage](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-im-recv-broadcast-message)

Usage example:

```js
zgEngine.on("onIMRecvBroadcastMessage",({roomID,messageList})=>{
  console.log(`onIMRecvBroadcastMessage: ${roomID},${messageList}`)
})
```

<a id="onIMRecvBarrageMessage"></a>

#### [onIMRecvBarrageMessage](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-im-recv-barrage-message)

Usage example:

```js
zgEngine.on("onIMRecvBarrageMessage",({roomID,messageList})=>{
  console.log(`onIMRecvBarrageMessage: ${roomID},${messageList}`)
})
```

<a id="onIMRecvCustomCommand"></a>

#### [onIMRecvCustomCommand](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-im-recv-custom-command)

Usage example:

```js
zgEngine.on("onIMRecvCustomCommand",({roomID,fromUser,command})=>{
  console.log(`onIMRecvCustomCommand: ${roomID},${fromUser},${command}`)
})
```

<a id="onCapturedDataRecordStateUpdate"></a>

#### [onCapturedDataRecordStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-captured-data-record-state-update)

Usage example:

```js
zgEngine.on("onCapturedDataRecordStateUpdate",({state,errorCode,config,channel})=>{
  console.log(`onCapturedDataRecordStateUpdate: ${state},${errorCode},${config},${channel}`)
})
```

<a id="onCapturedDataRecordProgressUpdate"></a>

#### [onCapturedDataRecordProgressUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-captured-data-record-progress-update)

Usage example:

```js
zgEngine.on("onCapturedDataRecordProgressUpdate",({progress,config,channel})=>{
  console.log(`onCapturedDataRecordProgressUpdate: ${progress},${config},${channel}`)
})
```

<a id="onNetworkModeChanged"></a>

#### [onNetworkModeChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-network-mode-changed)

Usage example:

```js
zgEngine.on("onNetworkModeChanged",({mode})=>{
  console.log(`onNetworkModeChanged: ${mode}`)
})
```

<a id="onNetworkSpeedTestError"></a>

#### [onNetworkSpeedTestError](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-network-speed-test-error)

Usage example:

```js
zgEngine.on("onNetworkSpeedTestError",({errorCode,type})=>{
  console.log(`onNetworkSpeedTestError: ${errorCode},${type}`)
})
```

<a id="onNetworkSpeedTestQualityUpdate"></a>

#### [onNetworkSpeedTestQualityUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-network-speed-test-quality-update)

Usage example:

```js
zgEngine.on("onNetworkSpeedTestQualityUpdate",({quality,type})=>{
  console.log(`onNetworkSpeedTestQualityUpdate: ${quality},${type}`)
})
```

<a id="onDownloadProgressUpdate"></a>

#### [onDownloadProgressUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-download-progress-update)

Usage example:

```js
zgEngine.on("onDownloadProgressUpdate",({resourceID,progressRate})=>{
  console.log(`onDownloadProgressUpdate: ${resourceID},${progressRate}`)
})
```

<a id="onCurrentPitchValueUpdate"></a>

#### [onCurrentPitchValueUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoExpressEngine#on-current-pitch-value-update)

Usage example:

```js
zgEngine.on("onCurrentPitchValueUpdate",({resourceID,currentDuration,pitchValue})=>{
  console.log(`onCurrentPitchValueUpdate: ${resourceID},${currentDuration},${pitchValue}`)
})
```


### ZegoExpressMediaPlayer
<a id="onMediaPlayerStateUpdate"></a>

#### [onMediaPlayerStateUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-state-update)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerStateUpdate",({state,errorCode})=>{
  console.log(`onMediaPlayerStateUpdate: ${state},${errorCode}`)
})
```

<a id="onMediaPlayerNetworkEvent"></a>

#### [onMediaPlayerNetworkEvent](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-network-event)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerNetworkEvent",({networkEvent})=>{
  console.log(`onMediaPlayerNetworkEvent: ${networkEvent}`)
})
```

<a id="onMediaPlayerPlayingProgress"></a>

#### [onMediaPlayerPlayingProgress](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-playing-progress)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerPlayingProgress",({millisecond})=>{
  console.log(`onMediaPlayerPlayingProgress: ${millisecond}`)
})
```

<a id="onMediaPlayerRenderingProgress"></a>

#### [onMediaPlayerRenderingProgress](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-rendering-progress)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerRenderingProgress",({millisecond})=>{
  console.log(`onMediaPlayerRenderingProgress: ${millisecond}`)
})
```

<a id="onMediaPlayerVideoSizeChanged"></a>

#### [onMediaPlayerVideoSizeChanged](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-video-size-changed)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerVideoSizeChanged",({width,height})=>{
  console.log(`onMediaPlayerVideoSizeChanged: ${width},${height}`)
})
```

<a id="onMediaPlayerLoadResource"></a>

#### [onMediaPlayerLoadResource](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-load-resource)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerLoadResource",({errorCode,seq})=>{
  console.log(`onMediaPlayerLoadResource: ${errorCode},${seq}`)
})
```

<a id="onMediaPlayerVideoData"></a>

#### [onMediaPlayerVideoData](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-video-data)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerVideoData",({videoParam})=>{
  console.log(`onMediaPlayerVideoData: ${videoParam}`)
})
```

<a id="onMediaPlayerRecvSEI"></a>

#### [onMediaPlayerRecvSEI](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-recv-sei)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerRecvSEI",({data})=>{
  console.log(`onMediaPlayerRecvSEI: ${data}`)
})
```

<a id="onMediaPlayerSoundLevelUpdate"></a>

#### [onMediaPlayerSoundLevelUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-sound-level-update)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerSoundLevelUpdate",({soundLevel})=>{
  console.log(`onMediaPlayerSoundLevelUpdate: ${soundLevel}`)
})
```

<a id="onMediaPlayerFrequencySpectrumUpdate"></a>

#### [onMediaPlayerFrequencySpectrumUpdate](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-frequency-spectrum-update)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerFrequencySpectrumUpdate",({spectrumList})=>{
  console.log(`onMediaPlayerFrequencySpectrumUpdate: ${spectrumList}`)
})
```

<a id="onMediaPlayerFirstFrameEvent"></a>

#### [onMediaPlayerFirstFrameEvent](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-first-frame-event)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerFirstFrameEvent",({event})=>{
  console.log(`onMediaPlayerFirstFrameEvent: ${event}`)
})
```

<a id="onMediaPlayerLocalCache"></a>

#### [onMediaPlayerLocalCache](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoMediaPlayer#on-media-player-local-cache)

Usage example:

```js
zgMediaPlayer.on("onMediaPlayerLocalCache",({errorCode,resource,cachedFile})=>{
  console.log(`onMediaPlayerLocalCache: ${errorCode},${resource},${cachedFile}`)
})
```

### ZegoExpressAudioEffectPlayer

<a id="onAudioEffectPlayStateUpdate"></a>

#### [onAudioEffectPlayStateUpdate](https://www.zegocloud.com/docs/api?doc=Express_Video_SDK_API~javascript_electron~class~ZegoAudioEffectPlayer#on-audio-effect-play-state-update)

Usage example:

```js
zgAudioEffectPlayer.on("onAudioEffectPlayStateUpdate",({audioEffectID,state,errorCode})=>{
  console.log(`onAudioEffectPlayStateUpdate: ${audioEffectID},${state},${errorCode}`)
})
```

### ZegoExpressScreenCaptureSource

<a id="onExceptionOccurred"></a>

#### [onExceptionOccurred](https://www.zegocloud.com/docs/api?doc=express-video-sdk_API~javascript_electron~class~ZegoScreenCaptureSource#on-exception-occurred)

Usage example:

```js
zgScreenCaptureSource.on("onExceptionOccurred",({exceptionType})=>{
  console.log(`onExceptionOccurred: ${exceptionType}`)
})
```

<a id="onWindowStateChanged"></a>

#### onWindowStateChanged

<Note title="Note">


  - Supported version: 3.4.0 and above.
  - Note: This callback takes effect only after calling the interface [on] to set it. To cancel the callback, please refer to [Cancel Callbacks](/real-time-video-electron-js/quick-start/setting-callback)
  - Usage limitation: Only applicable to Windows/macOS


</Note>





- Usage example:

    ```js
    zgScreenCaptureSource.on("onWindowStateChanged",({source,windowState,windowRect})=>{
      console.log(`onWindowStateChanged: ${source},${windowState},${windowRect}`)
    })
    ```

- Callback trigger timing: The state of the captured target window changes.

- Parameter information:


| Parameter | Type | Description |
| ---- | ---- | ---- |
| source | [ZegoAudioSourceType](https://www.zegocloud.com/docs/api?doc=Express_Video_SDK_API~javascript_electron~enum~ZegoAudioSourceType) | Screen capture source instance of the callback.|
| windowState | **ZegoScreenCaptureWindowState** | State of the captured window |
| windowRect | [ZegoRect](https://www.zegocloud.com/docs/api?doc=Express_Video_SDK_API~javascript_electron~struct~ZegoRect) | Captured window rectangle |

<Note title="Note">


**ZegoScreenCaptureWindowState** Screen capture source window state

Properties:

- ZegoScreenCaptureWindowStateOnScreen:
  > ZegoScreenCaptureWindowStateOnScreen = (0)
  > Window is on the current screen, coordinate area changes.

- ZegoScreenCaptureWindowStateOffScreen:
  > ZegoScreenCaptureWindowStateOffScreen = (1)
  > Window leaves the current screen, pause capture.

- ZegoScreenCaptureWindowStateDestroy:
  > ZegoScreenCaptureWindowStateDestroy = (2)
  > Window is destroyed.


</Note>



<a id="onRectChanged"></a>

#### onRectChanged

<Note title="Note">



  - Supported version: 3.7.0 and above.
  - Note: This callback takes effect only after calling the interface [on] to set it. To cancel the callback, please refer to [Cancel Callbacks](/real-time-video-electron-js/quick-start/setting-callback)
  - Usage limitation: Only applicable to Windows/macOS


</Note>



- Usage example:

    ```js
    zgScreenCaptureSource.on("onRectChanged",({source,captureRect})=>{
      console.log(`onRectChanged: ${source},${captureRect}`)
    })
    ```

- Callback trigger timing: The capture area changes.

- Parameter information:

| Parameter | Type | Description |
| ---- | ---- | ---- |
| source | [ZegoAudioSourceType](https://www.zegocloud.com/docs/api?doc=Express_Video_SDK_API~javascript_electron~enum~ZegoAudioSourceType) | Screen capture source instance of the callback.|
| captureRect | [ZegoRect](https://www.zegocloud.com/docs/api?doc=Express_Video_SDK_API~javascript_electron~struct~ZegoRect) | Captured area rectangle |



## Cancel Callbacks


To cancel registered callbacks, you can use the `off` and `removeAllListeners` methods to cancel registered callbacks.

| Method | Parameters | Description |
| ---- | ---- | ---- |
| off | eventName,listener | Cancel registered callbacks for specified events |
| removeAllListeners | eventName | Cancel all registered callbacks for specified events |

<Warning title="Note">


The on method can register multiple callbacks for the same event. When using on to register anonymous function callbacks, you can only use the removeAllListeners method to cancel all callbacks.

</Warning>




**Cancel Callback Example**

The following uses zgEngine's onDebugError event as an example,

Use the `off` method to cancel the `Listener` callback registered with `"onDebugError"`.
```js
const Listener = ({errorCode,funcName,info})=>{
  console.log(`onDebugError: ${errorCode},${funcName},${info}`)
}

zgEngine.on("onDebugError",Listener)

zgEngine.off("onDebugError",Listener)
```

Use the `removeAllListeners` method to cancel all callbacks registered with `"onDebugError"`.


```js
zgEngine.removeAllListeners("onDebugError")
```
