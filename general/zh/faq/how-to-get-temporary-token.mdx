---

date: "2025-09-12"
---
import { Title } from './title';
import TokenAssistant from './TokenAssistant.jsx';

<Title>如何获取临时 Token 用于调用 ZEGO SDK？</Title>



如下方表单所示，输入从 [ZEGO 控制台](https://console.zego.im/) 获取的有效 ServerSecret、AppID 以及 UserID、有效期（秒，可选 payload），点击“生成 Token”即可获得用于调试 ZEGO SDK 的临时Token：

<Warning title="注意">
临时 Token 仅用于测试验证。生产环境集成指引请参考：[使用 Token 鉴权](/real-time-video-ios-oc/communication/using-token-authentication)。
</Warning>

<TokenAssistant defaultTab="generate" />

