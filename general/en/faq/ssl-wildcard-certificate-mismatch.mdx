---

date: "2024-04-03"
ce-comparison:
  - "泛域名:wildcard domain"
  - "申请:apply"
  - "SSL:SSL"
  - "证书:certificate"
  - "控制台:console"
  - "上传:upload"
  - "提示:prompt"
  - "不匹配:mismatch"
---
import { Title } from './title';
import ArticleMetadata from '../../zh/faq/ArticleMetadata';

<Title>When using a wildcard domain to apply for an SSL certificate, why does the console prompt that the uploaded certificate does not match the domain when uploading the certificate?</Title>

<ArticleMetadata language="en" product="Video Call / Voice Call / Live streaming" platform="All" />




- - -

If you receive a prompt that the uploaded SSL certificate does not match the domain when uploading an SSL certificate in the [ZEGOCLOUD Console](https://console.zegocloud.com//), please check if it complies with the following rules:

- Generally has 1 wildcard `*` and starts with `*.`. For example, *.example.com, *.test.example.com, etc.
- Only supports same-level matching, not cross-level matching. For example: a digital certificate bound to the wildcard domain *.example.com supports p1.example.com, but does not support p1.p2.example.com.





