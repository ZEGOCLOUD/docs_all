---

date: "2022-04-29"
---
{/*本文仅限英文，无需同步中文*/}
import { Title } from './title';
import ArticleMetadata from '../../zh/faq/ArticleMetadata';

<Title>How to add the Maven repositories in Android Studio version bumblebee?</Title>

<ArticleMetadata language="en" product="Video Call / Voice Call / Live streaming" platform="Android" />

---


In Android Studio Bumblebee | 2021.1.1 Patch 1 and above, Maven's configuration `repositories` is migrated from `build.gradle` to  `setting.gradle`.
The following shows how to add it:

```groove
pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven { url 'https://www.jitpack.io' } // Add this line
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://www.jitpack.io' } // Add this line
    }
}
```


Other than the `repositories` configuration, the `buildscript` (e.g. `dependencies`) still needs to be added to the `build.gradle` file of your project.