---

date: "2022-11-16"
ce-comparison:
  - "两条:two"
  - "流:stream"
  - "混流:stream mixing"
  - "时:when"
  - "若:if"
  - "其中:among"
  - "一条:one"
  - "流:stream"
  - "断开:disconnect"
  - "了:past tense"
  - "混流:stream mixing"
  - "是否:whether"
  - "会:will"
  - "自动:automatically"
  - "停止:stop"
---
import { Title } from './title';
import ArticleMetadata from '../../zh/faq/ArticleMetadata';

<Title>When mixing two streams, if one of them disconnects, will the stream mixing automatically stop?</Title>

<ArticleMetadata language="en" product="Video Call / Voice Call / Live streaming" platform="All" />



- - -

The following situations exist:

- When initiating a stream mixing task, if any input stream does not exist, the stream mixing task initiation fails.
- After successfully initiating a stream mixing task, the SDK supports mixing single streams. If an input stream stops, the stream mixing task will automatically retry pulling the disconnected stream for 90 seconds (the disconnected stream image stays on the last frame); after 90 seconds, it will no longer retry. You need to manually call the [stopMixerTask ](https://www.zegocloud.com/article/api?doc=Express_Video_SDK_API~Java~class~im-zego-zegoexpress-zego-express-engine#stop-mixer-task) method to stop the stream mixing task.
- After successfully initiating a stream mixing task, if all input streams stop, the stream mixing task will automatically stop after 90 seconds.


