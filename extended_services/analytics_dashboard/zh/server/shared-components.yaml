openapi: 3.0.0
info:
  title: Analytics Dashboard Server API - 共享组件
  version: 1.0.0

# 共享的服务器配置（Analytics Dashboard）
servers:
  - url: https://analytics-api.zego.im
    description: 统一接入地址（不区分区域）
  - url: https://analytics-api-sha.zego.im
    description: 中国大陆（上海）
  - url: https://analytics-api-hkg.zego.im
    description: 港澳台（香港）
  - url: https://analytics-api-fra.zego.im
    description: 欧洲（法兰克福）
  - url: https://analytics-api-lax.zego.im
    description: 美西（加州）
  - url: https://analytics-api-bom.zego.im
    description: 亚太（孟买）
  - url: https://analytics-api-sgp.zego.im
    description: 东南亚（新加坡）

components:
  parameters:
    AppId:
      name: AppId
      in: query
      description: 💡公共参数。AppId，ZEGO 分配的用户唯一凭证。
      required: true
      schema:
        type: integer
        format: uint32

    SignatureNonce:
      name: SignatureNonce
      in: query
      description: 💡公共参数。16 位 16 进制随机字符串（8 字节随机数的 hex 编码）。
      required: true
      schema:
        type: string

    Timestamp:
      name: Timestamp
      in: query
      description: 💡公共参数。当前 Unix 时间戳，单位为秒。最多允许 10 分钟的误差。
      required: true
      schema:
        type: integer
        format: int64

    SignatureVersion:
      name: SignatureVersion
      in: query
      description: 💡公共参数。签名版本号，必须填写为 2.0。
      required: true
      schema:
        type: string
        default: "2.0"
        enum: ["2.0"]

    Signature:
      name: Signature
      in: query
      description: 💡公共参数。签名，用于验证请求的合法性。
      required: true
      schema:
        type: string

    IsTest:
      name: IsTest
      in: query
      description: 💡公共参数。是否为测试环境。取值如下：true（测试环境）、false（正式环境，默认值）。
      required: false
      schema:
        type: string
        default: "false"
        enum: ["true", "false"]

