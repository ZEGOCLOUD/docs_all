openapi: 3.0.0
info:
  title: Cloud Player Server API - 共享组件
  version: 1.0.0

# 共享的服务器配置
servers:
  - url: https://cloud-player-api.zego.im
    description: 统一接入地址

# 此文件仅用于定义共享组件，不包含具体的 API 端点
paths: {}

components:
  parameters:
    # 公共请求参数
    AppId:
      name: AppId
      in: query
      description: 💡公共参数。应用 Id，由 ZEGO 分配的用户唯一凭证。可从 <a target="_blank" href="https://console.zego.im/">ZEGO 控制台</a> 获取。
      required: true
      schema:
        type: number
        format: uint32

    SignatureNonce:
      name: SignatureNonce
      in: query
      description: 💡公共参数。16 位 16 进制随机字符串（8 字节随机数的 hex 编码）。生成算法可参考 <a href="/cloud-player-server/accessing-server-apis#signature-example">签名示例</a>。
      required: true
      schema:
        type: string

    Timestamp:
      name: Timestamp
      in: query
      description: 💡公共参数。当前 Unix 时间戳，单位为秒。生成算法可参考 <a href="/cloud-player-server/accessing-server-apis#signature-example">签名示例</a>，最多允许 10 分钟的误差。
      required: true
      schema:
        type: number
        format: int64

    SignatureVersion:
      name: SignatureVersion
      in: query
      description: 💡公共参数。签名版本号。
      required: true
      schema:
        type: string
        default: "2.0"
        enum: ["2.0"]

    Signature:
      name: Signature
      in: query
      description: 💡公共参数。签名，用于验证请求的合法性。请参考<a href="/cloud-player-server/accessing-server-apis#signature-mechanism">签名机制</a>生成。
      required: true
      schema:
        type: string

